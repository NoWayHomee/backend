DATABASE_URL="postgresql://nowayhome:nowayhome@localhost:5432/nowayhome?schema=public"
JWT_ACCESS_SECRET="change_me_access_secret"
JWT_REFRESH_SECRET="change_me_refresh_secret"
JWT_ACCESS_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"
PORT=3000
# Optional: enable Redis-backed cache. Without this, the app uses in-memory cache.
# REDIS_HOST=localhost
# REDIS_PORT=6379

POSTGRES_USER=nowayhome
POSTGRES_PASSWORD=nowayhome
POSTGRES_DB=nowayhome
# Đổi port này thành 5433, 5434... trên máy thành viên bị trùng port 5432
DB_LOCAL_PORT=5432